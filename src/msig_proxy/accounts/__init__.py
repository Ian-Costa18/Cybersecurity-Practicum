"""Identity & account management slice (see docs/source-layout.md).

Owns the Admin Portal, enrollment, the signing-key lifecycle, seeding, and the
read-only User Portal — the one sanctioned cross-cutting reader.
"""
